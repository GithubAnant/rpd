
import { HomeClient } from './HomeClient';

export default function HomePage() {
    return (
        <HomeClient
            userName={undefined}
            userImage={undefined}
            isGuest={true}
        />
    );
}
